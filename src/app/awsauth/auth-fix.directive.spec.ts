import { AuthFixDirective } from './auth-fix.directive';

describe('AuthFixDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthFixDirective();
    expect(directive).toBeTruthy();
  });
});
