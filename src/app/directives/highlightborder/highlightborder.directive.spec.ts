import { HighlightborderDirective } from './highlightborder.directive';

describe('HighlightborderDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightborderDirective(null);
    expect(directive).toBeTruthy();
  });
});
